# Here we define a default parameter.
def increament(number, by = 1):
    return number + by

print(increament(2)) # Return 3
print(increament(2,5)) #Return 7
# But optionally we can pass argument in default parameter.

